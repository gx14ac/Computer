/**
 * Demultiplexor:
 * {a, b} = {in, 0} if sel == 0
 *          {0, in} if sel == 1
 * selが0なら、aにinの値をbに０を. selが１ならaに0を、bにinを.
 */

 CHIP DMux {
     IN in, sel;
     OUT a, b;

     PARTS:
        
 }